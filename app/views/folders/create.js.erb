<% if flash.present? %>
  $("#modal-window").modal("hide");
  $(".message-container").remove();
  $("#reader").prepend("<%= escape_javascript(render 'layouts/messages') %>");
<% else %>
  $("#modal-window").html("<%= escape_javascript(render 'form') %>");
<% end %>